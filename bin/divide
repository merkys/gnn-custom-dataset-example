#!/usr/bin/perl

use strict;
use warnings;

open( my $TRAIN, '>', 'inputs/molecules-aromatic/training_data_files_prefixes.txt' );
open( my $TEST, '>', 'inputs/molecules-aromatic/testing_data_files_prefixes.txt' );
open( my $VALID, '>', 'inputs/molecules-aromatic/validation_data_files_prefixes.txt' );

while( <> ) {
    my $rand = rand;
    if( $rand < 4/6 ) {
        print $TRAIN $_;
    } elsif( $rand < 5/6 ) {
        print $TEST $_;
    } else {
        print $VALID $_;
    }
}

close $TRAIN;
close $TEST;
close $VALID;
