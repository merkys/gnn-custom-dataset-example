#!/usr/bin/perl

use strict;
use warnings;

open( my $TRAIN, '>', 'inputs/molecules-bond-sum/training_data_files_prefixes.txt' );
open( my $TEST, '>', 'inputs/molecules-bond-sum/testing_data_files_prefixes.txt' );
open( my $VALID, '>', 'inputs/molecules-bond-sum/validation_data_files_prefixes.txt' );

while( <> ) {
    my $rand = rand;
    if( $rand < 4/6 ) {
        print $TRAIN $_;
    } elsif( $rand < 5/6 ) {
        print $TEST $_;
    } else {
        print $VALID $_;
    }
}

close $TRAIN;
close $TEST;
close $VALID;
